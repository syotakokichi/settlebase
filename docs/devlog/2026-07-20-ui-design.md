# 2026-07-20 主要6画面のUI設計（Claude Design）

マルチテナント社内精算基盤デモの主要 6 画面（部門ウォレット / 振替・精算申請 /
承認 / 台帳 / 監査ログ / ロール管理）を Claude Design で設計した記録。
成果物は [docs/design/](../design/)（ワイヤー PNG 8 枚・台帳の静的 HTML・
[screen-specs](../design/screen-specs.md)・[design-system](../design/design-system.md)・
[concept](../design/concept.md)）。

## 1. ツール選定: Pencil.dev → Claude Design へ変更

当初計画は Pencil.dev だったが、着手時の比較（Pencil / Claude Design / HTML モック直書き）で
**Claude Design（Beta）主軸**に変更した。

なお、トークン管理・エクスポート・AI 連携は Pencil でも実現できる。両者の違いは
機能の有無より性格の違いで、次のように使い分けを整理した。

- **Pencil**: Figma に近い。キャンバスで多画面を本格的に編集し続けるならこちら
- **Claude Design**: とっかかりやすい。チャットに要件を投げれば形になり、
  デザインシステムも対話で立ち上がる

今回の決め手は (1) とっかかりやすさ（設計の立ち上がりが最速） (2) Claude Code との
同一エコシステム連携（DesignSync でデザインシステムを同期できる） (3) Beta 製品の
実践記録としての新規性。設計成果物は PNG / HTML にエクスポートして repo に置き、
Claude Design のプロジェクト自体は公開しない。

## 2. デザインシステム構築（トーン 3 案 → 採用 → 同期）

トーンの方向性を 3 案（A: 堅実・金融 = ネイビー高コントラスト / B: モダン SaaS =
ティール・余白広め / C: モノクロ+シグナル = 無彩色 + ステータス色のみ）作り分けて比較し、
**B 案を採用**（理由: 見た目の直感・オーナーのブランド感覚に合う）。候補はローカル生成した
スタイルタイル 3 枚をブラウザで並べて比較し、選定はオーナーが行った。

![デザイントーン候補 3 案の比較。左 = A 案「堅実・金融」、中央 = B 案「モダン SaaS」（緑枠 = 採用）、右 = C 案「モノクロ+シグナル」](../design/ds-tone-candidates.png)

採用トーンをコンポーネントプレビュー 7 点（カラー / タイポ / ボタン / バッジ / テーブル /
カード / サイドバー）に落とし、Claude Code 側から DesignSync ツールで Claude Design の
デザインシステムプロジェクト **settlebase-ds** へ同期した。**同期は成功し、fallback
（ローカル HTML モックへの切り替え）は発動していない**。同期直後にカード索引が生成されず
Design Systems パネルに表示されない問題があり、カードの明示登録（register）を行って
7 件の登録完了までは確認した（パネル表示の最終確認は未実施 — 下記「未実施・要フォロー」）。
トークン・密度・主要コンポーネントの永続版は [design-system.md](../design/design-system.md)
に書き起こしてあり、Claude Design 側の可用性に依存せず repo だけで読める。

### 未実施・要フォロー

- **Claude Design の Design Systems パネル表示スクリーンショット**: 未取得。
  上記のカード登録後の表示確認と、パネルのスクショ取得が残っている。
  本 devlog に載せているデザインシステムの証跡は、同期元となったトーン候補 3 案の
  スタイルタイル（上図）と [design-system.md](../design/design-system.md) の永続版のみ。

## 3. 画面要件仕様（screen-specs）と全体感の確定

ワイヤーを描く前に [screen-specs.md](../design/screen-specs.md) を作成し、
画面別の必須要素チェックリスト・ロールモデル 4 種（maker / checker / admin / viewer）を固定。
確認フォームでオーナーの赤入れを取り、全問原案どおり確定した
（ワイヤーの深さ = 主要状態のみ、空状態の描画は台帳のみ）。

並行して「そもそも何のシステムか」の全体感を詰め、[concept.md](../design/concept.md) に固定:

- **すべての残高変動をウォレット間の複式記帳（振替）1 種類に統一**
  （予算配賦 = 準備金→部門 / 立替精算 = 部門→メンバー / 逆仕訳 = 反対振替）
- 残高不足は申請時警告 + 承認時確定、マイナス残高は許容しない
- 承認と同時に自動記帳（1 トランザクション）
- **逆仕訳も申請→承認を通る**（訂正だけが maker-checker をバイパスする穴を作らない）
- 原資はテナント準備金ウォレット（唯一の承認外エントリは開設時の初期化のみ）
- 監査ログは「状態を変えた操作 + ログイン失敗」に限定
  （テナント切替などの閲覧系はアクセスログの領分として対象外）

## 4. ワイヤー生成（Claude Design・プロンプト 7 本）

Claude Design で新規プロジェクトを作成（Design system = settlebase-ds を適用、
Template = Wireframe、1280px デスクトップ固定）。プロンプトは 1 画面 1 本 + 仕上げ 1 本:

1. 共通シェル（テナント切替 + ナビ 7 項目 + ロールバッジ）+ 部門ウォレット
2. 振替・精算申請（冪等キーの説明表示 + 開発者向けキー確認 + 状態バッジ付き申請一覧）
3. 承認（maker-checker: 自分の申請は非活性 + 理由表示、承認は確認ステップ経由）
4. 台帳（append-only: 編集・削除ボタン非存在 + 逆仕訳の相互リンク + 空状態）
5. 監査ログ（操作種別フィルタ + 変更前後の展開表示 + 閲覧専用）
6. ロール管理（変更確認モーダル + 変更理由必須 + 最後の管理者の降格ガード）
7. 台帳のみ仕上げ精度へ（他はワイヤーのまま）

生成物は 1 つのインタラクティブな設計ファイルになり、ナビ切替・承認確認・空状態切替・
逆仕訳リンクのハイライトまで動く状態で出力された。

## 5. 適合チェックと修正（1 往復）

生成された設計ファイルを Claude Code 側から直接読み、screen-specs のチェックリストと
突き合わせて修正 8 点を 1 回の指示にまとめた:

1. **【境界】ダミーメールアドレスに実在ドメインが混入** → example.co.jp へ変更。
   Claude Design がアカウント情報から実在の所属ドメインを拾ったと推定される。
   **「AI が環境から実在情報を拾い込む」実例**であり、公開前チェックを設計段階から
   回すことの価値がそのまま示された
2. 台帳のボタン文言「逆仕訳を作成」→「逆仕訳を申請」（逆仕訳も承認を通る決定の反映）
3. 監査ログからテナント切替（フィルタ・ログ行）を削除（記録対象基準の確定を反映）
4. 部門ウォレット一覧の先頭に準備金ウォレット（バッジ + 「予算配賦の原資」注記）を追加
5. ウォレット作成ボタンに「管理者のみ」注記
6. 立替精算フォームを記帳モデルに整合（費用負担ウォレット選択 + 入金先 = 自分のウォレット固定）
7. 台帳の立替精算行の貸方をメンバーウォレット名に変更
8. ロール管理に「この画面は管理者のみ表示されます」注記

再チェックで 8 点すべての反映を確認した。

## 6. エクスポート（ローカルレンダリング + スクリーンショット）

設計ファイルはランタイム依存のため、ローカルで React ランタイムを補って file:// で描画し、
Chrome 経由で各画面の状態（承認詳細オープン・台帳空状態・ロール変更モーダル含む）を
PNG 化した。代表画面（台帳）はレンダリング済み DOM の静的スナップショットを
[ledger.html](../design/ledger.html)（自己完結・外部参照なし）として保存。

| ファイル | 内容 |
| --- | --- |
| [wire-01-wallets.png](../design/wire-01-wallets.png) | 部門ウォレット（準備金含む） |
| [wire-02-request.png](../design/wire-02-request.png) | 振替・精算申請（冪等キー説明） |
| [wire-03-approvals.png](../design/wire-03-approvals.png) | 承認（詳細パネル表示・自分の申請は非活性） |
| [wire-04-ledger.png](../design/wire-04-ledger.png) | 台帳（仕上げ精度・逆仕訳リンク） |
| [wire-04b-ledger-empty.png](../design/wire-04b-ledger-empty.png) | 台帳の空状態 |
| [wire-05-audit.png](../design/wire-05-audit.png) | 監査ログ（変更前後の展開表示） |
| [wire-06-roles.png](../design/wire-06-roles.png) | ロール管理（降格ガード） |
| [wire-06b-role-modal.png](../design/wire-06b-role-modal.png) | ロール変更の確認モーダル |

代表画面（台帳・仕上げ精度）。append-only の注記、逆仕訳の相互リンク、
編集・削除ボタンが存在しないこと、正味の集計行が 1 枚で読める:

![台帳画面。append-only の注記と「逆仕訳を申請」ボタン、逆仕訳エントリ（LED-0422）と元エントリ（LED-0419）の相互リンク、表示範囲の正味合計を表示](../design/wire-04-ledger.png)

## 7. 適合表（screen-specs チェックリストとの突き合わせ）

状態系（空 / エラー / 送信中等）は確定方針どおり「仕様書の記述で充足」とする
（ワイヤー描画は台帳の空状態のみ）。

分母は [screen-specs](../design/screen-specs.md) の各画面のチェック項目数と一致する
（共通シェル 4 / 部門ウォレット 6 / 振替・精算申請 6 / 承認 6 / 台帳 7 / 監査ログ 6 /
ロール管理 6）。

| 画面 | 充足 | 特記 |
| --- | --- | --- |
| 共通シェル | 4/4 | ロール差分は代表ロール（承認者）視点 + 注記方式 |
| 部門ウォレット | 5/6 | 未充足 1 = ウォレット詳細ビュー。ワイヤー対象外と判断（実装フェーズの縦切りで設計。台帳のウォレットフィルタが代替導線） |
| 振替・精算申請 | 6/6 | 冪等キーの説明 + 開発者向けキー確認導線あり |
| 承認 | 6/6（※） | maker-checker 非活性 + 理由表示・確認ステップあり。※ 詳細パネル内のダミーデータに下記の既知の残があるが、チェック項目（詳細パネルの存在と表示項目）自体は充足 |
| 台帳 | 7/7 | 逆仕訳の相互リンク・合計行（正味）・空状態あり |
| 監査ログ | 6/6 | 記録対象 = 状態変更 + ログイン失敗に限定 |
| ロール管理 | 6/6 | 最後の管理者の降格ガード・変更理由必須あり |

既知の残課題（実装フェーズで解消）:

- 承認詳細の立替精算の「振替元 → 先」表記が旧形式（`― → 部門`）のまま。
  記帳モデル上は「部門 → メンバー」になる（ダミーデータの細部のため修正往復はしない）
- [design-system.md](../design/design-system.md) のタイポグラフィ目標スケール
  （見出し L 24px / 本文 14px）と、ワイヤー実測（見出し L 20px / 本文・ナビ 13px）が
  1〜2 段ずれている。実装で Tailwind テーマへ反映する際にどちらへ寄せるかは未確定

## 8. 境界チェック（push 前）

1. 禁止語 grep: repo 全体で 0 件（`grep -rn -e <禁止語リスト> . --exclude-dir=.git --exclude-dir=node_modules`
   にて確認。exit 1 = ヒット 0 件）。**禁止語リスト自体は公開 repo に書けないため
   `<禁止語リスト>` でマスクしている**（計画では実行コマンドの固定を求めていたが、
   リストの公開を避ける判断でこの部分のみマスクした）
2. 公開前チェック 4 点: (1) プロンプト・仕様は自作 (2) スクショは自分の環境・自作物のみ
   (3) メンバー投稿・外部やり取りの引用なし (4) 有料コンテンツをなぞっていない — OK
3. PNG 目視: 全 8 枚を確認。人名・部門・金額・メールはすべて架空
   （メールドメインは example.co.jp、§5-1 の混入は修正済み）
4. HTML（ledger.html）: 外部参照・スクリプト・`<link>`/`<img>`/`@import`・コメント・
   メタデータのいずれも持たない自己完結ファイルであることを確認

### 対象コミットと CI

| commit | 内容 | harness | db-tests |
| --- | --- | --- | --- |
| `5830110` | UI 設計の追加（design 一式 + devlog） | [failure](https://github.com/syotakokichi/settlebase/actions/runs/29753867686)（devlog 行頭 `+` の markdownlint MD004） | [success](https://github.com/syotakokichi/settlebase/actions/runs/29753867479) |
| `58fcf25` | 上記 markdownlint エラーの修正 | [success](https://github.com/syotakokichi/settlebase/actions/runs/29753980987) | [success](https://github.com/syotakokichi/settlebase/actions/runs/29753980948) |
| `1c399d2` | グラレコ追加 + README 更新（最終 push） | [success](https://github.com/syotakokichi/settlebase/actions/runs/29755382607) | [success](https://github.com/syotakokichi/settlebase/actions/runs/29755382585) |

## 学び

1. **設計ツールにも境界チェックが要る**: AI デザインツールはアカウント情報等の
   実在情報をダミーデータに拾い込むことがある。公開物は生成のたびに禁止語・実在情報の
   チェックを通す（§5-1）
2. **要件の言語化 → 生成 → 適合チェックの型が効く**: チェックリスト付きの仕様
   （screen-specs）を先に固定したことで、生成物の抜け（準備金・監査ログの範囲）を
   機械的に検出でき、修正が 1 往復で済んだ
3. **「すべては振替」の統一が画面を単純にした**: 予算配賦・立替精算・逆仕訳を
   同じ記帳プリミティブに寄せたことで、フォームと台帳の表現が 1 種類で済んでいる
